# bootc-image-builder config for the bare-metal/VM installer ISO.
#
# COSMIC ships its own first-boot wizard, cosmic-initial-setup (packaged for
# Fedora: https://packages.fedoraproject.org/pkgs/cosmic-initial-setup/), the
# COSMIC equivalent of gnome-initial-setup. It handles user creation and
# locale/network setup after Anaconda finishes installing the base OS, so we
# hand those off to it instead of letting Anaconda do them.

[customizations.installer.kickstart]
contents = """
%post
bootc switch --mutate-in-place --transport registry ghcr.io/corruptedbit/fedora-dev-cosmic:latest
%end
"""

[customizations.installer.modules]
# Trimmed module set: cosmic-initial-setup handles user/network/timezone setup
# on first boot, so Anaconda only needs to partition the disk.
enable = [
  "org.fedoraproject.Anaconda.Modules.Storage",
  "org.fedoraproject.Anaconda.Modules.Runtime"
]

disable = [
  "org.fedoraproject.Anaconda.Modules.Network",
  "org.fedoraproject.Anaconda.Modules.Security",
  "org.fedoraproject.Anaconda.Modules.Services",
  "org.fedoraproject.Anaconda.Modules.Users",
  "org.fedoraproject.Anaconda.Modules.Subscription",
  "org.fedoraproject.Anaconda.Modules.Timezone"
]
